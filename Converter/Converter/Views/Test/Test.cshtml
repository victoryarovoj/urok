@model Converter.Models.Category

@{
    ViewBag.Title = "Test";
}

<h2>Test</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Category</h4>
        <hr />

        <div class="form-group">
            @Html.LabelFor(model => model.CategoryId, "CategoryId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("CategoryId", (IEnumerable<SelectListItem>)ViewData["Types"])
                @Html.DropDownListFor(model => model.Category1, (IEnumerable<SelectListItem>)ViewData["Types1"])
                

                <input type="text" name="TB" />
                <input type="submit" />
                @ViewBag.MyParam
            </div>
        </div>

        @*<div class="form-group">
            @Html.LabelFor(model => model.Category1, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("Category1", (IEnumerable<SelectListItem>)ViewData["Types1"])
            </div>
        </div>*@
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
